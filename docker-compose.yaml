version: "3.4"
services:
  app:
    container_name: app-instance
    image: app-image
    build:
      target: debug
    volumes:
    - ./src/:/work/src/
    ports:
      - 5000:5000 # webserver
      - 9229:9229 # debug
  